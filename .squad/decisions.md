# Squad Decisions

## Active Decisions

- **2026-06-10 — Hugo rebuild strategy:** Rebuild the blog as a custom in-repo Hugo site rather than adopting a third-party theme. Preserve existing Markdown posts, `/posts/<slug>` URLs where possible, `/images/...` asset paths, all existing images, and `images/logo.png` unchanged.
- **2026-06-10 — AI content publishing model:** Weekly Microsoft Foundry generation must create draft posts, generated images, metadata, and a pull request for human review. It must not auto-publish directly to the main branch.
- **2026-06-10 — Visual modernization constraint:** Modernize the look and feel with progressive parallax and scroll reveal, but preserve the current logo and keep motion accessible via `prefers-reduced-motion` support.
- **2026-06-10 — Hugo migration validation:** Hugo is installed locally under `.tools/hugo/` and ignored by Git. All 36 Jekyll posts have migrated Hugo counterparts under `content/posts/`; `_posts/` remains unchanged. `tools/validate_hugo_migration.py --warn-unmigrated` and `.tools/hugo/hugo.exe --gc --minify --destination public-hugo-preview` pass. Production cutover remains gated on staging/preview smoke tests.
- **2026-06-15 — Post figure lightbox (default-on, opt-out):** Post figures opt into a lightweight vanilla-JS lightbox by default — replacing the legacy "Haz click para ver la imagen más grande." caption with a visual zoom-icon overlay plus click-to-enlarge behavior. Per-post escape hatch via `lightbox="false"`. Vanilla solution chosen over Lity (needs jQuery, never loaded) to cut dependencies; accessibility (focus handling, keyboard close) addressed in review. Approvals: Wanda (visual/a11y), Natasha (final, after focus-handling revisions), Bruce (final JS revisions). Files: layouts/shortcodes/figure.html, layouts/_default/single.html, assets/css/main.css, assets/js/hugo-main.js; lity.js left unused.
- **2026-06-22 — Hugo build BLOCKED (Natasha — validation rejected):** Supersedes the 2026-06-10 "all-green" validation status for the current template state. `hugo --minify` fails at `layouts/shortcodes/responsive-embed.html:5:1` (unterminated quoted string). `layouts/index.searchindex.json` also has malformed Hugo delimiters (`{ {- ... - } }` instead of valid `{{- ... -}}`) — a likely second blocker once the shortcode is fixed. Stale public page `/posts/compartimoss-unplugged-ai-02/` shows the YouTube iframe but the player reports `Error 153` (`embedder.identity.missing.referrer`); local-server validation blocked by the build failure. No files modified — fix required before re-validation. **→ RESOLVED 2026-06-23 (see entry below).**
- **2026-06-23 — AI article pipeline MVP parameters (Rodrigo approved ALL 6 arch decisions):** Engine, trigger, models and architecture for the AI-assisted article generation pipeline for CODERTECTURA-Blog.
  - **Engine / trigger:** Runs on GitHub Actions. MVP uses MANUAL `on: workflow_dispatch` only; scheduled `on: schedule` / cron (e.g. Mon 06:00) is deferred to the supervised phase — a one-line YAML swap, no architecture change. (cron is a trigger type *inside* Actions, not an alternative to it.)
  - **Models (Foundry / Azure):** Cover images → `MAI-Image-2.5`. Text (research + drafting) → `GPT-5.4-mini` as a SINGLE tier in the MVP (no premium cascade). A higher-tier polish pass may be added later if PR-review quality lags. PENDING ACTION (Bruce): verify regional availability, `api-version` and pricing of both models in Foundry before Phase 0 — names/prices are Rodrigo's choice and must be checked against official Foundry pages. Cross-agent (Wanda): image model changed `gpt-image-1` → `MAI-Image-2.5`; art direction and prompt / negative-prompt templates must be re-validated against MAI-Image-2.5 capabilities.
  - **Approved architecture decisions:** **#1** Execution topology — start with DIRECT MVP (direct model calls; promotion to Foundry hosted agents deferred until prompts stabilize). **#2** Bot identity — least-privilege GitHub App; cannot self-approve PRs; branch protection requires Rodrigo's human approval. **#3** Publishing semantics — TWO-LOCK model (approved PR merge + `draft:false` + `ai.reviewed_by` to render); merging alone does not publish. **#5** Source allow-list + novelty threshold — approved (concrete authoritative Microsoft feeds and similarity cutoff to be detailed in Phase 0). **#6** Cadence / throttling / fail-closed — approved (1–2 per week, max concurrent AI PRs, kill-switch, no PR if any gate including budget fails).
  - **#4 Topic ledger — APPROVED (Rodrigo confirmed 2026-06-23):** `automation/topics/<id>.yaml` (one file per topic) + rebuildable embeddings cache + published index derived live from content/posts/. Lifecycle candidate→queued→in_review→published/rejected/parked; double-key dedup (exact slug/title + semantic embeddings) with novelty threshold 0.82. **All 6 architecture decisions now FIRM; design closed.**
  - **Phase 0 started (no generation):** Bruce scaffolded the ledger on disk — `automation/topics/.gitkeep`, `automation/topics/_example.yaml` (schema template; scanner ignores `_`-prefixed files), `automation/README.md` (ledger data contract). Pending Rodrigo-only prerequisites: least-privilege GitHub App, branch protection on main, CODEOWNERS, authoritative verification of MAI-Image-2.5 + GPT-5.4-mini in his Foundry tenant. Open question to Rodrigo: continue with workflow skeleton + gate scripts, or do GitHub App + branch protection first?
- **2026-06-23 — Hugo build blocker RESOLVED (supersedes Natasha 2026-06-22):** The 2026-06-22 build blocker is resolved. The two cited files were fixed: `layouts/shortcodes/responsive-embed.html` is now clean (line 5 is a well-formed multiline `slice`, no unterminated string), and `layouts/index.searchindex` now uses correct `{{- -}}` delimiters — it was renamed from `.json` to no-extension (with an inline comment) so JSON formatters can no longer rewrite Go template delimiters. Rodrigo verified `.\.tools\hugo\hugo.exe server --minify -D` compiles with no errors on 2026-06-23; coordinator independently inspected both files and confirmed they are clean. The earlier exit-code-1 seen in terminal was an unrelated typo (`ckear`), not the build. The Hugo quality-gate for the AI article pipeline is operative.

## Governance

- All meaningful changes require team consensus
- Document architectural decisions here
- Keep history focused on work, decisions focused on direction
